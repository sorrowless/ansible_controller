---
# Iptables rules
iptables:
  default_allowed_tcp_ports: [22, 443, 80]
  raw_rules:
  - "-A INPUT -s 10.10.10.0/24 -j ACCEPT -m comment --comment 'docker-prom_network'"
  - "-A INPUT -s {{ ansible_ip }}/24 -p tcp --dport 9100 -j ACCEPT -m comment --comment 'node-exporter_self_access'"
