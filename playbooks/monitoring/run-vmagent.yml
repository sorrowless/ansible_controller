#!/usr/bin/env -S ansible-playbook -e @vars/extra.yaml
#
---
- name: Install and configure VMAgent
  hosts: [monitoring_servers, monitoring_clients, partial_monitoring_clients]
  become: yes
  roles:
    - { role: sorrowless.vmagent, tags: ["server", "vm", "vmagent"] }
